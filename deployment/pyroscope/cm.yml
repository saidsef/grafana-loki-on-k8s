---
apiVersion: v1
kind: ConfigMap
metadata:
  name: config
  labels:
    app.kubernetes.io/name: pyroscope
data:
  overrides.yaml: |
    overrides:
      {}
  config.yaml: |
    analytics:
      reporting_enabled: false

    log_level: info

    self_profiling:
      disable_push: true
